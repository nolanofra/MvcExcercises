@model IEnumerable<MvcApplicationFirstSample.ViewModels.AlbumViewModel>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
    <head>
        <title>title</title>
    </head>
    <body>
        @foreach (var album in Model)
        {
            <div>@album.Title</div>
            <div>
                @album.LoggedUser
            </div>
        }
</body>
</html>